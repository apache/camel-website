{
  "scripts": {
    "build": "(cd antora-ui-camel && yarn gulp pack) && antora --redirect-facility disabled site.yml && hugo --minify"
  },
  "devDependencies": {
    "@antora/cli": "^2.0.0-beta.1",
    "@antora/site-generator-default": "^2.0.0-beta.1",
    "hugo-bin": "^0.39.0"
  }
}
